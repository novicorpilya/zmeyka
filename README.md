# 🐍 Zmeyka - Senior-Level AI Education Platform

**Zmeyka** — это высокотехнологичная образовательная платформа с искусственным интеллектом для обучения программированию. Продукт построен с использованием архитектуры уровня **Senior Production**, обеспечивающей масштабируемость, безопасность и исключительный пользовательский опыт.

---

## 🛠 Технологический Стек (Production Ready)

### Frontend (Modern & Scalable)
- **Framework**: [Nuxt 3](https://nuxt.com/) (Vue 3 + TypeScript)
- **Architecture**: **FSD (Feature-Sliced Design)** — модульная структура для легкой поддержки и масштабирования.
- **State Management**: [Pinia](https://pinia.vuejs.org/) (с поддержкой сторов для сущностей и фич).
- **Styling**: [Tailwind CSS](https://tailwindcss.com/) + GSAP для премиальных анимаций.
- **Authentication**: JWT (Access + Refresh Tokens) + HTTP-only Cookies.
- **Client**: Fetch API wrapper с автоматической обработкой токенов и ошибок.

### Backend (Secure & Robust)
- **Framework**: [NestJS](https://nestjs.com/) (Node.js) — Enterprise-level фреймворк.
- **Database**: [PostgreSQL](https://www.postgresql.org/) (через **Prisma ORM**). Локально используется SQLite.
- **Security**: 
  - Пароли: Bcrypt хеширование.
  - Авторизация: Passport.js (JWT Strategy).
  - Безопасность сессий: Refresh Token rotation stored in DB.
- **API Documentation**: [Swagger (OpenAPI 3.0)](https://swagger.io/) — доступно по адресу `/docs`.
- **Validation**: Class-validator + ValidationPipes.

### AI & Integrations
- **AI Core**: **Google Gemini Pro API** — проверка домашних заданий, генерация фидбека и персональные рекомендации.
- **Payments**: **Stripe** (тестовые подписки и разовые покупки).
- **Communication**: WebSockets (Coming Soon) для real-time статусов проверки кода.

---

## 🏗 Архитектура проекта (FSD)

Мы используем методологию **Feature-Sliced Design**, что делает проект понятным для больших команд:

```text
frontend/src/
├── app/            # Инициализация (styles, providers)
├── pages/          # Страницы (login, register, dashboard, course)
├── widgets/        # Крупные блоки (Header, CourseCard)
├── features/       # Бизнес-логика (auth, homework-submission)
├── entities/       # Сущности (user, course, lesson)
└── shared/         # Общие компоненты, api-client, types
```

---

## 🚀 Как запустить

### 1. Backend
```bash
cd backend
npm install
# Настройте .env (JWT_SECRET, DATABASE_URL)
npm run prisma:push   # Синхронизация базы
npm run start:dev     # Запуск на http://localhost:3001
```

### 2. Frontend
```bash
cd frontend
npm install
# Настройте .env (NUXT_PUBLIC_API_BASE)
npm run dev           # Запуск на http://localhost:3000
```

---

## 🔑 Ключевые возможности (MVP)

1.  **Smart Auth**: Регистрация и вход с защитой от XSS через HTTP-only куки.
2.  **AI Checker**: Автоматическая проверка кода пользователем через Gemini API с подробным объяснением ошибок.
3.  **Course Builder**: Удобный интерфейс для создания курсов (Teacher-only).
4.  **Premium Dashboard**: Личный кабинет студента с прогрессом и сертификатами.

---

## 🎨 Дизайн-философия
Мы верим, что обучение должно быть красивым. 
- **Dark-friendly core**: Глубокие оттенки Slate и Indigo.
- **Glassmorphism**: Эффект матового стекла на панелях.
- **Micro-interactions**: Каждый клик и переход сопровождается плавной анимацией GSAP.

---
Разработано с ❤️ и фокусом на Senior-level архитектуру.
